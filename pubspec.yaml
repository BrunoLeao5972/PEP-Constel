name: kds_constel
description: A new Flutter project for Kitchen Display System.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^2.4.0
  intl: ^0.18.1
  google_fonts: ^6.1.0
  mongo_dart: ^0.10.3
  bcrypt: ^1.1.3
  shared_preferences: ^2.5.3
  esc_pos_utils_plus: ^2.0.4
  win32: ^5.5.4
  ffi: ^2.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
